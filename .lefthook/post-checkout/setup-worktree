#!/bin/sh
set -e
# $1 = previous HEAD, $2 = new HEAD, $3 = 1 (branch checkout) or 0 (file checkout)
[ "$3" = "1" ] || exit 0
git rev-parse --git-dir | grep -q '/worktrees/' || exit 0

echo "Worktree detected, running setup..."
mise trust
mise install
bun install
